{{>admin_navigation}}
<div class="container p-4">
    <div class="row">
        {{#each lista_productos}}
         <div class="col-md-9">
        <table class="table table-striped table-bordered">
            <thead>
             <tr>
                 <th>Nombre</th>
                 <th>Descripcion</th>
                 <th>Marca</th>
                 <th>Opcion</th>
            </tr>
            </thead>
            <tr>
                <td>{{nombre}}</td>
                <td>{{descripcioncorta}}</td>
                <td>{{marca_id}}</td>
                <td>
                    <a href="/empresa/productos/delete/{{id}}" class="btn btn-danger">Eliminar</a>
                    <a href="/empresa/productos/editar/{{id}}" class="btn btn-secondary">Editar</a>
                </td>
            </tr>
        </table>
          </div>
        {{/each}}
    </div>
</div>
