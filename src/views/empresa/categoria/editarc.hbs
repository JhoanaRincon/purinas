{{>admin_navigation}}
<div class="container p-4"> 
    <div class="row">
        <div class="col.md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/empresa/categoria/editarc/{{cat.id}}" method="POST">
                    <div class="form-group">
                            <input type="number" value="{{cat.id}}" class="form-control" name="id" placeholder="Id" autofocus>
                        </div>
                        <div class="form-group">
                            <input type="text" value="{{cat.descripcion}}" class="form-control" name="descripcion" placeholder="Descripcion" >
                        </div>
                        <div class="form-group">
                            <input type="text" value="{{cat.estado}}" class="form-control" name="estado" placeholder="Estado">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">SAVE</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>