{{>admin_navigation}}
<div class="container p-4">
    <div class="row">
        {{#each lista_categoria}}
         <div class="col-md-9">
        <table class="table table-striped table-bordered">
            <thead>
             <tr>
                 <th>ID</th>
                 <th>Descripcion</th>
                 <th>Estado</th>
                 <th>Opciones</th>
             </tr>
            </thead>
            <tr>
                <td>{{id}}</td>
                <td>{{descripcion}}</td>
                <td>{{estado}}</td>
                <td>
                    <a href="/empresa/categoria/delete/{{id}}" class="btn btn-danger">Eliminar</a>
                    <a href="/empresa/categoria/editarc/{{id}}" class="btn btn-secondary">Editar</a>
                </td>

            </tr>
        </table>
          </div>
        {{/each}}
    </div>
</div>